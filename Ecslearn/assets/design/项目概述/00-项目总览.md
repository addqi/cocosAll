# 00 - 肉鸽游戏策划案：项目总览

## 我们要做什么

一款 **Cocos Creator 3.x 2D 俯视角肉鸽射击游戏**，核心玩法参考「幽灵射手」（Archero 类）：

- 玩家操控角色在有限地图中移动
- 停下时自动攻击最近的敌人
- 清完一层的敌人后通过传送门进入下一层
- 每层结束后随机三选一获得技能/增益
- 死亡后重头开始，部分成长保留（Meta 进度）

**与参考项目的关键差异**：我们基于已有的 **ECS 架构 + Buff 系统 + 属性系统**来构建，而不是幽灵射手的「全局 GameManager + 散落 Component」模式。数据结构先行，代码自然清晰。

---

## 当前实现进度

> 最后更新：2026-04-10

| 模块 | 对应文档 | 状态 | 说明 |
|------|---------|------|------|
| **ECS 框架** | — | ✅ 完成 | Entity/Component/System 全套 |
| **属性系统** | — | ✅ 完成 | 8 个属性 JSON + GeneralPropertyMgr + 修饰器链 |
| **Buff 系统** | — | ✅ 完成 | BuffFactory + 声明式 Effect + AttributeChangeResolver |
| **输入系统** | 08 | ✅ 完成 | 4 层管线 + Proxy Entity + 鼠标/键盘 |
| **玩家角色** | 02 | ✅ 完成 | 移动 + FSM(Idle/Run/Shoot) + 属性 + 战斗 + 动画 |
| **弹射物** | 02 | ✅ 完成 | ArrowProjectile + ProjectilePool |
| **敌人基础** | 02 | ✅ 完成 | EnemyControl + Combat + Property + BuffOwner |
| **Buff 效果** | 05/07 | ⏳ 部分 | 6 个效果类已实现（属性修改 + 回血），行为/触发类未做 |
| **战斗演示** | 07 | ✅ 完成 | BattleTestPanel（5 buff 按钮 + 双方状态） |
| **游戏循环** | 01 | ⏳ 部分 | GameLoop 存在但无 GameStateMachine，无场景分层 |
| **敌人 AI** | 02 | ❌ 未开始 | 无 AISystem / 行为状态机 |
| **地图系统** | 03 | ❌ 未开始 | 仅占位 index.ts |
| **肉鸽机制** | 04 | ❌ 未开始 | 仅占位 index.ts |
| **SkillManager** | 05 | ❌ 未开始 | 技能→Buff 胶水层未实现 |
| **UI 系统** | 06 | ❌ 未开始 | 仅占位 index.ts，无 HUD/菜单/技能面板 |

---

## 参考项目「幽灵射手」核心拆解

| 系统 | 幽灵射手做法 | 我们的改进方向 |
|------|-------------|---------------|
| **架构** | GameManager 静态单例持有所有引用 | ECS：Entity + Component + System 分离 |
| **移动** | 虚拟摇杆 → RigidBody.move | 摇杆 → InputComponent → MovementSystem |
| **攻击** | Player 脚本内 update 检测 + 动画回调射箭 | CombatSystem 统一处理攻击节奏与弹道 |
| **敌人** | Monster/Boss 各自脚本，GameManager.arrMonster 数组管理 | ECS Entity + AIComponent，System 统一调度 |
| **技能** | PlayerData.arrSkill + parsePlayerSkill 重算属性 | Buff 系统 + 属性修饰器链，干净解耦 |
| **关卡** | CSV checkpoint 固定配置，map 表随机选一张 | 房间模板 + 程序化组合，更「肉鸽」|
| **数据** | CSV 文件 + LocalConfig 全局加载 | JSON 配置 + TypeScript 类型安全 |

---

## 已有资产盘点

项目 `Ecslearn` 里已经有这些可直接复用的东西：

### 基础系统（`script/baseSystem/`）

| 模块 | 路径 | 状态 |
|------|------|------|
| **ECS 框架** | `baseSystem/ecs/` | ✅ Entity/Component/System 全套 |
| **Buff 系统** | `baseSystem/buff/` | ✅ BuffFactory + BuffEffectBase + BuffRuntimeInfo + 装饰器注册 |
| **属性系统** | `baseSystem/properties/` | ✅ BaseValue + ComputeValue + Modifier 链 + GeneralPropertyMgr |
| **FSM** | `baseSystem/fsm/` | ✅ StateMachine 通用状态机 |
| **事件总线** | `baseSystem/util/EventBus.ts` | ✅ 解耦通信 |
| **动画** | `baseSystem/animation/` | ✅ SpriteAnimator + SpriteSheetUtil |
| **资源管理** | `baseSystem/resource/ResourceMgr.ts` | ✅ 预加载 + 缓存 |

### 游戏逻辑（`script/game/`）

| 模块 | 路径 | 状态 |
|------|------|------|
| **游戏核心** | `game/core/` | ✅ GameLoop（4 系统管线） + World（实体注册表） |
| **输入管线** | `game/system/` + `game/component/` | ✅ RawInput→ActionMap→PlayerControl→MoveSync |
| **玩家** | `game/player/` | ✅ Control + FSM(Idle/Run/Shoot) + Combat + Property + Animation |
| **敌人** | `game/enemy/` | ✅ Control + Combat + Property + BuffOwner + Animation |
| **弹射物** | `game/projectile/` | ✅ ArrowProjectile + ProjectilePool |
| **实体桥接** | `game/entity/` | ✅ EntityPropertyMgr + EntityBuffMgr + AttributeChangeResolver |
| **Buff 效果** | `game/skill/effects/` | ⏳ 6 个效果类（SimpleAttrBuff 基类 + 5 子类 + RegenEffect） |
| **配置** | `game/config/` | ✅ 8 个属性 JSON + 5 个 Buff JSON + 属性枚举 |
| **测试** | `game/test/` | ✅ BattleTestPanel（战斗全流程测试） |
| **地图** | `game/map/` | ❌ 仅占位 |
| **肉鸽** | `game/roguelike/` | ❌ 仅占位 |
| **技能管理** | `game/skill/` | ❌ SkillManager / SkillSelector 未实现 |
| **UI** | `script/ui/` | ❌ 仅占位 |

### 美术资源（`res/`）

| 资源 | 说明 |
|------|------|
| **Tiny Swords 素材包** | 完整的中世纪像素风：建筑、地形、UI、多兵种单位 |
| **角色帧动画** | `02_Stripes/` 下 Idle/Run/Attack/Damage 等序列帧 |
| **道具图标** | `gold/` 下武器图标 |
| **UI 元素** | Tiny Swords/UI Elements 下按钮、血条、横幅等 |

> **关键判断**：基础设施和单兵战斗已完成。现在缺的是「游戏流程胶水层」—— GameStateMachine、地图/关卡管理、肉鸽技能选择、UI 界面。

---

## 技术选型决策

### 用 ECS，但不要过度 ECS

ECS 用在它该用的地方：

- **实体管理**：玩家、敌人、弹道、拾取物 → Entity + Component
- **行为系统**：移动、碰撞、战斗、AI → System
- **属性计算**：基础值 + 修饰器 → 已有属性系统

**不用 ECS 的地方**：

- UI 管理（Cocos 节点树天然适合）
- 场景切换 / 游戏状态（简单状态机够了）
- 音效 / 粒子特效（Cocos 原生 API 直接用）

### 数据格式：JSON > CSV

幽灵射手用 CSV，我们用 JSON：
- TypeScript 天然解析，不需要额外 CSV 解析器
- 支持嵌套结构，描述技能效果更自然
- 已有配置文件（`buffConfig/`、`propertyConfig/`）都是 JSON

---

## 游戏核心数据结构（最重要的事）

> "Bad programmers worry about the code. Good programmers worry about data structures."

在写任何代码之前，先把核心数据结构定义清楚：

```typescript
/** 一局游戏的运行时状态 */
interface RunState {
    floor: number;              // 当前层数
    roomIndex: number;          // 当前房间索引
    phase: 'combat' | 'reward' | 'shop' | 'transition';
    enemiesAlive: number;       // 剩余敌人数
}

/** 玩家局内状态（死亡清零） */
interface RunPlayerState {
    hp: number;
    maxHp: number;
    gold: number;
    skills: SkillId[];          // 本局获得的技能
    buffs: BuffId[];            // 当前激活的 Buff
}

/** 玩家持久化状态（跨局保留）*/
interface MetaPlayerState {
    totalGold: number;
    highestFloor: number;
    unlockedSkills: SkillId[];  // 解锁池
    permanentUpgrades: Record<UpgradeId, number>;
}
```

这三个结构是整个游戏的**数据根节点**。所有系统都围绕它们运转。

---

## 文档导航（按开发阶段排列）

### 已完成 ✅

| 文档 | 内容 |
|------|------|
| [08-输入系统架构](../已完成/08-输入系统架构.md) | 4 层输入管线、Proxy Entity、键鼠映射 |
| [07-战斗系统实战](../已完成/07-战斗系统实战.md) | 属性×Buff×战斗统合演示、BattleTestPanel |

### 阶段 1：角色战斗 ← 当前

完善玩家 / 敌人 / Boss，补全技能系统与行为类 Buff。

| 文档 | 内容 | 状态 |
|------|------|------|
| [02-角色与战斗系统](../阶段1-角色战斗/02-角色与战斗系统.md) | 敌人 AI、Boss、死亡循环、多敌人类型 | ⏳ 核心完成 / AI 未做 |
| [05-Buff与技能系统](../阶段1-角色战斗/05-Buff与技能系统.md) | SkillManager、行为类 Buff、触发类 Buff | ⏳ 6 效果类 / 胶水层未做 |

### 阶段 2：关卡系统

房间模板、多种关卡类型（战斗 / 奖励 / Boss / 恢复 / 商店）、层流转。

| 文档 | 内容 | 状态 |
|------|------|------|
| [03-地图与关卡系统](../阶段2-关卡系统/03-地图与关卡系统.md) | 房间模板、敌人生成、传送门、难度曲线 | ❌ 未开始 |

### 阶段 3：游戏整体

状态机、肉鸽循环、UI、存档 —— 串成完整游戏。

| 文档 | 内容 | 状态 |
|------|------|------|
| [01-核心游戏循环](../阶段3-游戏整体/01-核心游戏循环.md) | GameStateMachine、场景分层、关卡生命周期 | ⏳ GameLoop✅ / 状态机❌ |
| [04-肉鸽核心机制](../阶段3-游戏整体/04-肉鸽核心机制.md) | 技能三选一、死亡循环、Meta 进度、商店 | ❌ 未开始 |
| [06-UI与数据驱动](../阶段3-游戏整体/06-UI与数据驱动.md) | HUD、菜单、技能面板、摇杆、存档 | ❌ 未开始 |

**开发顺序**：已完成 → 阶段 1（02→05）→ 阶段 2（03）→ 阶段 3（01→04→06）

---

## 一句话总结

> ECS 骨架、Buff 引擎、属性系统、输入管线、玩家/敌人/弹射物 —— 战斗单元已经能跑。下一步：GameStateMachine + 场景分层 + 地图系统 + 肉鸽技能循环 + UI。
